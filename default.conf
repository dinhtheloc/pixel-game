server {
  listen 80;

  location /mango-portal {
    root /usr/share/nginx/html;
    port_in_redirect off;
    absolute_redirect off;
    index /mango-portal/index.html;
    try_files $uri $uri/ /mango-portal/index.html =404;
  }

  location / {
    return 404;
  }
}
